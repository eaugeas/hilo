from setuptools import setup

setup(
    name='hilo_cmd',
    version='0.1',
    description='hilo command line library',
    author='eaugeas',
    author_email='eaugeas@gmail.com',
    packages=['hilo_cmd'],
    install_requires=[],
)
